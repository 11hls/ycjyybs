# 个人网站部署指南

> 项目路径：`D:\个人网站`
> 线上地址：https://ycjyybs.pages.dev
> 云平台：Cloudflare Pages + D1 数据库

---

## 一、项目结构

```
D:\个人网站\
├── index.html          # 首页（个人介绍 + 留言板）
├── flappy.html         # 游戏页（Flappy Bird）
├── tailwind.css        # 本地化 Tailwind 样式（13.6KB，已压缩）
├── bg.mp4              # 首页背景视频（25MB，当前未上传，有渐变兜底）
├── favicon.svg         # 站点图标
├── resume-1.webp       # 简历图片（WebP 格式，217KB）
├── resume.pdf          # 简历 PDF（1MB）
├── schema.sql          # D1 数据库建表脚本
├── migrate.sql         # 数据库迁移脚本（增量更新用）
├── _headers            # Cloudflare 安全头 + 缓存策略
├── _redirects          # URL 重定向规则（当前为空）
├── wrangler.toml       # Cloudflare 配置（项目名 + D1 绑定）
├── cf-token.txt        # Cloudflare API 凭证（不上传，仅本地用）
├── deploy.sh           # 一键部署脚本（Git Bash 运行）
├── deploy.cmd          # Windows 双击入口（调用 deploy.sh）
├── functions/
│   └── api/
│       ├── messages.js  # 留言板 API（增删查 + 点赞 + 速率限制）
│       └── scores.js    # 游戏排行榜 API
├── input.css           # Tailwind 源文件
├── tailwind.config.js  # Tailwind 配置
├── package.json        # npm 依赖声明
└── node_modules/       # 依赖（不上传）
```

---

## 二、前提条件

### 1. Node.js（>= 18，推荐 24 LTS）
```powershell
node -v   # 检查版本
```

### 2. Cloudflare 账号
- 注册：https://dash.cloudflare.com/sign-up
- 需要：API Token + Account ID + 项目名

### 3. cf-token.txt（凭证文件，三行）
```
第1行：CLOUDFLARE_API_TOKEN
第2行：CLOUDFLARE_ACCOUNT_ID
第3行：Pages 项目名（如 ycjyybs）
```

**获取方式：**
1. 登录 Cloudflare → 右上角头像 → My Profile → API Tokens
2. 点 "Create Token" → 选 "Edit Cloudflare Workers" 模板
3. 在 Account 级别勾选：Cloudflare Pages (Edit)、D1 (Edit)
4. 创建后复制 Token（只显示一次！）
5. Account ID 在 Dashboard 右侧栏 → Account ID 处复制

---

## 三、一键部署（推荐）

### 方法 A：双击 deploy.cmd
直接双击 `D:\个人网站\deploy.cmd`，会自动调用 Git Bash 运行 deploy.sh。

### 方法 B：命令行运行
```powershell
# 在项目目录下
& 'C:\Program Files\Git\bin\bash.exe' ./deploy.sh
```

### 部署脚本做了什么
1. 读取 `cf-token.txt` 获取凭证
2. 创建临时目录，只拷贝白名单文件（排除 node_modules、package.json 等）
3. 调用 `wrangler pages deploy` 上传到 Cloudflare Pages
4. 上传完成后自动清理临时目录

### 白名单文件（deploy.sh 第 27 行）
```
index.html  flappy.html  bg.mp4  tailwind.css  favicon.svg
resume-1.webp  resume.pdf  wrangler.toml  schema.sql  _headers  _redirects
functions/  （整个目录）
```

> **注意：** bg.mp4（25MB）上传会非常慢甚至超时。如不需要视频背景，
> 从白名单中删掉 `bg.mp4` 即可，index.html 有渐变背景兜底。

---

## 四、手动部署（单条命令）

如果 deploy.sh 出问题，可以手动执行：

```powershell
# 1. 设置凭证
$token = Get-Content 'D:\个人网站\cf-token.txt'
$env:CLOUDFLARE_API_TOKEN = $token[0]
$env:CLOUDFLARE_ACCOUNT_ID = $token[1]
$proj = $token[2]

# 2. 创建临时目录并拷贝文件
$tmp = Join-Path $env:TEMP "cf_deploy_$(Get-Random)"
New-Item -ItemType Directory -Path $tmp -Force | Out-Null
$files = @('index.html','flappy.html','tailwind.css','favicon.svg',
            'resume-1.webp','resume.pdf','wrangler.toml','schema.sql',
            '_headers','_redirects')
foreach ($f in $files) { Copy-Item "D:\个人网站\$f" $tmp }
Copy-Item 'D:\个人网站\functions' $tmp -Recurse

# 3. 上传
& 'D:\个人网站\node_modules\.bin\wrangler.cmd' pages deploy $tmp --project-name $proj --branch main

# 4. 清理
Remove-Item $tmp -Recurse -Force
```

---

## 五、数据库（D1）操作

### 首次建表
```powershell
$env:CLOUDFLARE_API_TOKEN = (Get-Content 'D:\个人网站\cf-token.txt')[0]
$env:CLOUDFLARE_ACCOUNT_ID = (Get-Content 'D:\个人网站\cf-token.txt')[1]

& 'D:\个人网站\node_modules\.bin\wrangler.cmd' d1 execute blog-db `
    --remote --file='D:\个人网站\schema.sql'
```

### 查看表结构
```powershell
& 'D:\个人网站\node_modules\.bin\wrangler.cmd' d1 execute blog-db `
    --remote --command='PRAGMA table_info(messages)'
```

### 增量迁移（添加字段）
```powershell
& 'D:\个人网站\node_modules\.bin\wrangler.cmd' d1 execute blog-db `
    --remote --file='D:\个人网站\migrate.sql'
```

---

## 六、常见问题

### Q1：上传很慢 / 卡住
**原因：** bg.mp4 有 25MB，Cloudflare Pages 单文件上限 25MB，
上传大文件受网络波动影响极大。

**解决：**
- 从 deploy.sh 白名单删掉 `bg.mp4`（首页有渐变背景兜底）
- 或压缩视频到 5MB 以下再上传

### Q2：EPERM 写日志报错
```
Failed to write to log file EPERM: operation not permitted
```
**原因：** wrangler 要写日志到 `C:\Users\...\xdg.config\.wrangler\logs\`，
沙箱或权限不足。

**解决：** 部署本身会成功（报错只在写日志时），忽略即可。
如要消除，以管理员身份运行 PowerShell。

### Q3：PowerShell 不支持 `&&` 和 `bash`
**原因：** PowerShell 5.x 不支持 `&&` 语法；`bash` 命令不在 PATH 中。

**解决：**
- 用 `;` 代替 `&&`
- Git Bash 路径：`& 'C:\Program Files\Git\bin\bash.exe'`
- 或直接用 deploy.cmd 双击运行

### Q4：wrangler 要求 Node 22+
**原因：** 新版 wrangler 要求 Node.js >= 22。

**解决：**
```powershell
# 查看当前版本
node -v
# 如 < 22，去 https://nodejs.org 下载 LTS 版安装
```

### Q5：留言板报错 "留言太快啦"
**原因：** API 有 IP 速率限制——60 秒内最多 2 条留言。

**解决：** 等一分钟再发，或修改 `functions/api/messages.js` 中的阈值。

### Q6：/flappy 页面 404 或重定向循环
**原因：** _redirects 中的重定向规则与 Cloudflare 默认行为冲突。

**解决：** _redirects 留空（只保留注释），Cloudflare 默认支持
`/flappy` → `flappy.html`（省略 .html 扩展名）。

### Q7：cf-token.txt 丢失
**解决：** 去 Cloudflare Dashboard 重新生成 API Token，
按格式写入三行即可。

---

## 七、日常更新流程

改了代码后，只需一条命令：

```powershell
# 方法 1：双击 deploy.cmd
# 方法 2：命令行
& 'C:\Program Files\Git\bin\bash.exe' 'D:\个人网站\deploy.sh'
```

如果只改了 HTML/CSS/JS（没改数据库），不需要执行 SQL。
如果改了 schema.sql，需要手动跑迁移。

---

## 八、安全注意事项

1. **cf-token.txt 绝不上传**：deploy.sh 会先将它移到 /tmp，
   上传完再恢复。手动部署时不要把它拷到临时目录。
2. **API Token 权限最小化**：只需 Pages Edit + D1 Edit，
   不要给 Zone 级别权限。
3. **_headers 已配置安全头**：
   - X-Frame-Options: SAMEORIGIN（防点击劫持）
   - X-Content-Type-Options: nosniff（防 MIME 嗅探）
   - Referrer-Policy: strict-origin-when-cross-origin
   - Permissions-Policy: 禁用摄像头/麦克风/定位

---

## 九、本地开发预览

```powershell
# 安装依赖（首次）
cd D:\个人网站
npm install

# 本地预览（带 Functions 本地运行）
& '.\node_modules\.bin\wrangler.cmd' pages dev . --local
# 打开 http://localhost:8788
```

> 本地预览会用本地 D1 数据库（在 .wrangler/ 下），
> 线上数据不会受影响。

---

## 十、关键版本信息（2026-09-04）

| 组件 | 版本 | 说明 |
|------|------|------|
| Node.js | 18.20.4（待升级到 24 LTS） | wrangler 3.78 兼容 Node 18 |
| wrangler | 3.78.0 | 专为 Node 18 兼容选择 |
| tailwindcss | 3.4.13 | 已本地化构建 |
| Cloudflare Pages | — | 项目名 ycjyybs |
| D1 数据库 | blog-db | ID: c4e92951-965d-497b-8e7d-4ffb4ca2f92b |
