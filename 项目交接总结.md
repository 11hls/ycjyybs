# 「一只蛙的数值崩坏」项目交接总结

> 用途：新开对话时贴给 AI，或让它直接读本文件，即可无缝接手。
> 项目目录：`D:\个人网站`　线上：https://ycjyybs.pages.dev

---

## 一、部署铁律（最重要）

**只在用户明确说「推 CF / 推上去 / 部署」时才用 wrangler 部署。平时改动只改本地文件，绝不自动部署。**

- 凭证：`D:\个人网站\cf-token.txt`（第 1 行 token、第 2 行 account_id、第 3 行项目名 `ycjyybs`）
- 部署命令：`wrangler pages deploy --branch main --commit-dirty=true`
- 发布文件清单（拷贝到临时目录再 deploy）：
  `index.html`、`flappy.html`、`resume.pdf`、`resume-1.png`、`wrangler.toml`、`schema.sql`、`functions/`、`favicon.svg`
- 清理临时目录用 git bash `rm -rf`（PowerShell 删中文路径会静默失败）。

---

## 二、文件结构

| 文件 | 说明 |
|---|---|
| `flappy.html` | 单文件 HTML5 Canvas 游戏（逻辑坐标 400×600，物理 800×1200，`ctx.scale(2,2)`） |
| `index.html` | 网站首页，游戏卡片入口 |
| `favicon.svg` | 矢量图标（蓝紫圆角背景 + 白色「余」字），index/flappy 双页引用 |
| `functions/api/scores.js` | 排行榜后端（CF Pages Function + D1），POST 返回 `{ok, rank}`，GET 返回榜单 |

---

## 三、游戏功能全貌（均已实现）

- **游戏名**：一只蛙的数值崩坏；**介绍**：在数值崩坏的世界里，你是一只青蛙。
- **布局**：左道具图鉴 / 中游戏区 / 右 TOP10 排行榜（常驻，30 秒自动刷新）。
- **道具**（图鉴排序：血包→生质液→虚化→万年灵乳→加倍）：
  - 血包：回血；满血吃→分数翻倍；每局限量 20 个，刷满降级为金币。
  - 生质液：生命上限 +1（封顶 8），最多吃 5 个（吃满后不再加上限，改 +2 分），canvas 矢量绘制（无白底）。
  - 虚化：3 秒虚化；万年灵乳：穿竹得分永久翻倍（`pipeMult *= 2`，绿瓶 ×2）；加倍：当前分数翻倍。
- **金币效果**：获得「穿过两次竹子」的分数 = `2 * pipeMult`（跟随万年灵乳叠加）。
- **血条**：`❤ 当前/上限` 格式；无「最高」显示（已删 best 变量）。
- **竹子间距 4 档**：超窄 95~105(12%) / 窄 115~135 / 中 145~170 / 宽 175~195。
- **死亡弹窗**：标题「少侠殒命于此，是否再战？」；输入框 placeholder「少侠，报上名来」；无历史最高。
- **PK**：按钮「⚔️ 上榜 PK」；PK 结果 4 种展示（无排名 / 第 1 名 / TOP10 / 十名开外「无名小卒」）。
- **排行榜**：固定 10 行撑满高度，空位「虚位以待」，仅展示前 10 名；同名去重（保留最高分），后端 `GROUP BY nick + MAX(score)`，前端 `topTen`/`localPk` 同样去重。
- **localStorage**：昵称记忆 `flappyNick`；无后端时降级本地榜。

---

## 四、部署历史（最新在最前）

| 部署号 | 内容 |
|---|---|
| `86f84356` | 手机端放开 `body` 滚动可上下滑（`@media` 内 `overflow-y:auto` + order 排序：游戏→图鉴→排行榜）+ 点击蓝一下修复（`-webkit-tap-highlight-color:transparent`）+ 主页新增「小游戏」版块（关于我/留言板之间，卡片跳 flappy）+ 导航小游戏/留言板位置互换（桌面+移动端） |
| `7ca82bca` | 飘字真正修复：`dt` 单位是帧（非秒），`life` 改 96 帧=1.6s、上移 0.6 逻辑像素/帧，飘字不再一闪而过 |
| `1e8d3924` | 排行榜宽度 216→240px，昵称/分数字号 13→12px |
| `5a663004` | 排行榜同名去重：后端 GET 改 `GROUP BY nick + MAX(score)`，POST 排名改去重子查询；前端 `topTen`/`localPk` 同步去重保留最高分 |
| `cbfa065d` | 道具调整：金币改「穿竹两次分数」；生质液改「最多吃 5 个」（不限刷新）；加一药水改名「万年灵乳」，效果改穿竹得分永久翻倍 |
| `3462db1f` | 删除「八股复活」玩法（revive 按钮 + quiz 弹窗 + QUESTS 题库 + doRevive 等，共 203 行） |
| `16e42266` | 继续修复答题弹窗按钮无响应：`.quiz-panel` 加 `pointer-events:auto`，确保弹窗面板内部按钮能被点击 |
| `661396b6` | 修复答题弹窗「提交答案」按钮无响应：`.overlay` 加 `pointer-events:none`、`.overlay.show` 加 `pointer-events:auto` |
| `5ec10fdc` | PK 上传失败改为 toast 提示（不再改按钮文字）；网络断开提示「网络卡顿」不降级本地榜；死亡标题加问号 |
| `cb12fd63` | 答题提交按钮（quizSubmit/submitAnswer/selectOption）、favicon.svg 矢量图标 |
| `0e10fe7e` | 八股复活玩法（50 题题库 + 答题复活按钮） |
| `89e485dc` | 无名小卒 / 超窄档 / 删最高 / 3秒虚化 / HUD 血量 |
| `7d6a1222` | 新名字新介绍 / 死亡文案 / 血包翻倍 / 删竹顶圆 |
| `8bfd349c` | 游戏名 / 生质液降级加一 / 血包限量 20 / 右侧榜 |

---

## 五、踩坑经验（新对话务必记住）

1. **编辑含中文/全角标点的文件**：用 Python 按 UTF-8 读取 `replace()` 再写回，比 Edit 工具可靠；Edit 匹配含全角标点的字符串易失败。
2. **校验中文题面**：检查串需带全角标点（如题面结尾「？」），否则误报 NO。
3. **bash 的 curl 能联网但不能写文件**；下载/验证线上文件用 Python `urllib`。
4. **Windows 下 `/tmp` 被解析成 `D:\tmp`**；临时文件放当前工作目录。
5. **PowerShell 删含中文路径会静默失败**；删文件用 git bash `rm -rf`。
6. **线上验证 flappy.html 需带浏览器 UA**，默认 UA 会被 CF 403；`index.html` 和 `favicon.svg` 正常 200。
7. **统计题库数量**用 `re.findall(r"q: '([^']+)'", html)`，当前应为 50。
