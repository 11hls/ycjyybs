# 「余常进的个人网站」项目交接总结

> 用途：新开对话时贴给 AI，或让它直接读本文件，即可无缝接手。
> 项目目录：`D:\个人网站`　线上：https://ycjyybs.pages.dev
> 文档文件名：`项目交接文档.md`

---

## 一、部署铁律（最重要）

**只在用户明确说「推 CF / 推上去 / 部署 / 上线」时才用 wrangler 部署。平时改动只改本地文件，绝不自动部署。**

- 凭证：`D:\个人网站\cf-token.txt`（第 1 行 token、第 2 行 account_id、第 3 行项目名 `ycjyybs`）
- 部署命令：`bash deploy.sh`（已改为白名单部署，自动拷贝必要文件到临时目录再 deploy）
- 白名单文件清单：
  `index.html`、`flappy.html`、`xiong.mp4`、`bg-meteor.mp4`、`bg-aot-ocean.jpg`、`tailwind.css`、`favicon.svg`、`resume-1.webp`、`resume.pdf`、`wrangler.toml`、`schema.sql`、`_headers`、`_redirects`、`functions/`、`游戏音效/`
- **不传**：`package.json`、`node_modules/`、`input.css`、`tailwind.config.js`、`migrate.sql`、`项目交接文档.md`、`deploy.sh`、`deploy.cmd`、`cf-token.txt`、`.wrangler/`
- 清理临时目录用 git bash `rm -rf`（PowerShell 删中文路径会静默失败）。

### 沙箱环境下部署 wrangler 的环境变量重定向（重要）

在 TRAE 沙箱中，wrangler 默认会向 `C:\Users\<用户>\AppData\Roaming\xdg.config\.wrangler\logs\` 和项目目录 `.wrangler\tmp\` 写日志/临时文件，会被沙箱拦截（EPERM）。解决方法：在执行 wrangler 前设置以下环境变量到工作目录内：

```powershell
$env:XDG_CONFIG_HOME = "<工作目录>\.xdg"
$env:WRANGLER_HOME    = "<工作目录>\.wrangler-home"
```

并确保 `cf-token.txt` 的内容分别赋给 `CLOUDFLARE_API_TOKEN` 和 `CLOUDFLARE_ACCOUNT_ID`。部署完成后清理 `.xdg`、`.wrangler-home`、`deploy-tmp` 等临时目录。

---

## 二、文件结构

| 文件 | 说明 |
|---|---|
| `index.html` | 网站首页，游戏卡片入口；Tailwind 已本地化（引用 tailwind.css） |
| `flappy.html` | 单文件 HTML5 Canvas 游戏（逻辑坐标 700×600，物理 1400×1200，`ctx.scale(2,2)`） |
| `xiong.mp4` | 首页背景视频（自嘲小熊游泳，默认，1280×720 24fps，约 1.16MB） |
| `bg-meteor.mp4` | 陨石背景视频（1280×720 24fps，约 0.33MB，已压缩） |
| `bg-aot-ocean.jpg` | 进击的巨人背景图（1920×1080，约 0.17MB，已压缩；从 Steam 工坊 2499859642 scene.pkg 提取的 JPG，海洋+人物场景） |（来自 Steam Wallpaper Engine 工坊，路径 `E:\SteamLibrary\steamapps\workshop\content\431960\3753326745\xiong.mp4`，约 18MB） |
| `bear-bg.mp4` | 旧熊熊背景视频（备用，当前未使用） |
| `bg.mp4` | 旧 CDN 背景视频（已被 xiong.mp4 替代，备用） |
| `tailwind.css` | Tailwind 构建产物（已 minify，约 13KB；源：`input.css` + `tailwind.config.js`） |
| `favicon.svg` | 矢量图标（蓝色 #3b82f6 圆角背景 + 白色「余」字） |
| `_headers` | Cloudflare Pages 安全头 + 缓存策略 |
| `_redirects` | 干净 URL：`/flappy` → `/flappy.html` |
| `schema.sql` | 完整建表脚本（含 likes/ip 字段、msg_likes 表，用于新部署） |
| `migrate.sql` | 线上迁移脚本（已有 messages 表时执行一次：加 likes/ip 字段、建 msg_likes 表） |
| `functions/api/scores.js` | 排行榜后端（CF Pages Function + D1），POST 返回 `{ok, rank}`，GET 返回榜单 |
| `functions/api/messages.js` | 留言板后端（GET 列表 + POST 提交 + PUT 点赞切换；POST 含 60s/2 条 IP 速率限制） |
| `游戏音效/` | 青蛙游戏音效文件夹（8 个道具音效 + 1 个背景音乐，详见第三节） |

### 构建工具（不部署）
| 文件 | 说明 |
|---|---|
| `package.json` | tailwindcss 依赖声明 |
| `tailwind.config.js` | Tailwind 配置（colors/fontFamily，扫描 index.html + flappy.html） |
| `input.css` | `@tailwind base/components/utilities` 指令 |

**重新构建 tailwind.css**：`cd D:\个人网站 && npx tailwindcss -i input.css -o tailwind.css --minify`

---

## 三、游戏功能全貌（均已实现）

- **游戏名**：一只蛙的数值崩坏；**介绍**：在数值崩坏的世界里，你是一只青蛙。
- **布局**：左道具图鉴 / 中游戏区 / 右 TOP10 排行榜（常驻，30 秒自动刷新）。
- **道具**（图鉴排序：金币→血包→生质液→虚化→万年灵乳→加倍）：
  - 金币：获得「穿过两次竹子」的分数 = `2 * pipeMult`（跟随万年灵乳叠加）。
  - 血包：回血；满血吃→分数翻倍；每局限量 20 个，刷满降级为金币。
  - 生质液：生命上限 +1（封顶 8），最多吃 5 个（吃满后不再加上限，改 +2 分），canvas 矢量绘制（无白底）。
  - 虚化：3 秒虚化；万年灵乳：穿竹得分永久翻倍（`pipeMult *= 2`，绿瓶 ×2）；加倍：当前分数翻倍。
- **血条**：`❤ 当前/上限` 格式；无「最高」显示（已删 best 变量）。
- **竹子间距 4 档**：超窄 95~105(12%) / 窄 115~135 / 中 145~170 / 宽 175~195。
- **死亡弹窗**：标题「少侠殒命于此，是否再战？」；输入框 placeholder「少侠，报上名来」；无历史最高。
- **PK**：按钮「⚔️ 上榜 PK」；PK 结果 4 种展示（无排名 / 第 1 名 / TOP10 / 十名开外「无名小卒」）。
- **排行榜**：固定 10 行撑满高度，空位「虚位以待」，仅展示前 10 名；同名去重（保留最高分），后端 `GROUP BY nick + MAX(score)`，前端 `topTen`/`localPk` 同样去重。
- **localStorage**：昵称记忆 `flappyNick`；无后端时降级本地榜。
- **音效系统**（2026-09-05 新增）：外部音效文件替代原 Web Audio 合成音效。
  - 文件位置：`游戏音效/`（已加入 deploy 白名单）
  - 音效映射：跳跃→青蛙跳跃.mp3、金币→金币音效.mp3、回血→回血.mp3、满血吃血包→双倍药水.mp3、生质液→生质液.mp3、万年灵乳→回满血.mp3、虚化药水→虚化药水.mp3、加倍药水→双倍药水.mp3
  - 受伤/死亡：保留原合成音效
  - 穿柱得分：不播音效
  - 并发播放：`playSound()` 用 `cloneNode()` 创建新 Audio 实例，多个音效可同时触发
  - 背景音乐：Funny game（音量 0.15，循环播放，首次跳跃时开始，死亡时暂停）
  - 静音按钮控制所有音频（含背景音乐）

---

## 四、首页 UI 现状（2026-09-05 更新）

- **背景视频**：`xiong.mp4`（来自 Steam Wallpaper Engine 工坊 3753326745，已压缩至 1.16MB），autoplay muted loop playsinline，onerror 降级为径向渐变背景。
- **蒙版暗度**：`.video-veil` 默认 30%（渐变 `rgba(9,9,11,.38)→.30→.40` + 纯色层 `rgba(9,9,11,.30)`），无用户调节滑杆。
- **右上角按钮**：「联系我」使用 `.animated-button`（Uiverse 风格：双 SVG 箭头 + 圆形扩散动效）；Hero 区「查看简历」使用 `.button`（青色描边 + hover 扫光填充）。两者样式已互换。
- **导航栏**：`h-20`（80px），`.nav-pill` padding `.6rem 1.4rem`、字号 `.9rem`。
- **卡片边框**：`.vcard` 默认边框 `1px solid rgba(59,130,246,.35)`（淡蓝），hover 加深到 `.7`；毛玻璃 `--glass-blur: 8px`（原 0px）。
- **字体**：使用系统字体栈（无外部 Google Fonts 依赖）。
- **留言板换行修复**（2026-09-05）：`.msg-content` 加 `line-break:anywhere`，解决连续全角标点（如「！！！」）导致的 7 字提前换行问题。
- **换背景功能**（2026-09-05）：右上角调色板按钮，点击即循环切换 3 个背景（🐻自嘲小熊视频 / ☄️陨石视频 / ⚔️进击的巨人海洋图片），切换时弹出 toast 提示；背景选择通过 localStorage 持久化。已删除暗度滑杆，使用默认 30% 遮罩。
- **下雨开关**（2026-09-05 新增）：右上角云朵按钮，点击切换 Canvas 雨滴动画的开/关，默认开启；按钮 active 状态高亮；开关状态通过 localStorage（`rainOn`）持久化。下雨 IIFE 暴露 `window.setRain(bool)` 接口。
- **兴趣爱好模块**（2026-09-05 改版）：4 个兴趣爱好改为 2×2 3D 翻转卡片网格。
  - 正面：大图标 + 标题；背面：描述文字；hover 时 rotateY(180deg) 翻转
  - 卡片高度 180px，间距 28px，4 张卡片各有主题边框色（蓝/紫/绿/橙）
  - 上方有「兴趣爱好」标题，与自我介绍用 mt-16 间距分隔
  - 移动端自动变单列

---

## 五、部署历史（最新在最前）

| 部署号 | 内容 |
|---|---|
| `97bb8a4a` (2026-09-05) | 背景资源压缩：bg-meteor.mp4 24.38→0.33MB、xiong.mp4 18.15→1.16MB、bg-aot-ocean.jpg 1.24→0.17MB（ffmpeg 1280×720 24fps CRF30 + sharp jpeg q72） |
| `c4795efa` (2026-09-05) | 背景换海洋图（名称仍「进击的巨人」）；自嘲小熊→「自嘲小熊游泳中」；删除亮度滑杆；加下雨开关；兴趣爱好拆为独立版块；导航栏 fixed 顶到顶部+移动端全显示+联系我按钮缩小；关于我文案丰富为5段；删除hero区重复介绍 |
| `9e077c89` (2026-09-05) | 青蛙游戏加背景音乐（Funny game，音量 0.15）；背景音乐音量从 0.3 调至 0.15 |
| `38b5ed9b` (2026-09-05) | 青蛙游戏音效系统上线（8 个道具音效 + 背景音乐）；音效并发播放修复（cloneNode）；生质液音效；背景音乐去掉 |
| `457a3a28` (2026-09-05) | 视频背景换为 xiong.mp4（Steam 工坊 3753326745）；蒙版暗度默认 30%；右上角「联系我」与「查看简历」按钮样式互换；导航栏 h-16→h-20；vcard 毛玻璃 0→8px + 淡蓝边框；deploy.sh 白名单 bg.mp4→xiong.mp4、resume-1.png→resume-1.webp |
| 待部署 | 修 Bug 1~4 + 任务 5~16：schema 加 likes/ip + msg_likes 表；messages.js content 300 + 60s/2 条限速 + ip 插入；index.html loadMessages 改用 #msgCount + div 闭合；Tailwind 本地化（tailwind.css 13KB）；bg.mp4 视频本地化 + onerror 降级；favicon 配色统一蓝；index/flappy 加 OG/Twitter meta + theme-color；_headers（安全头+缓存）+ _redirects（/flappy）；留言板移动端 min-height 分档；flappy stage 加 :has(.legend.collapsed) 更宽公式；deploy.sh 白名单部署 |
| `86f84356` | 手机端放开 `body` 滚动可上下滑 + 主页新增「小游戏」版块 + 导航位置互换 |
| `7ca82bca` | 飘字真正修复：`dt` 单位是帧（非秒），`life` 改 96 帧=1.6s |
| `5a663004` | 排行榜同名去重：后端 GET 改 `GROUP BY nick + MAX(score)` |
| `cbfa065d` | 道具调整：金币改「穿竹两次分数」；生质液改「最多吃 5 个」；加一药水改名「万年灵乳」 |
| `3462db1f` | 删除「八股复活」玩法（共 203 行） |

---

## 六、踩坑经验（新对话务必记住）

1. **编辑含中文/全角标点的文件**：用 Python 按 UTF-8 读取 `replace()` 再写回，比 Edit 工具可靠；Edit 匹配含全角标点的字符串易失败。也可用 Node.js `fs.readFileSync/writeFileSync`（UTF-16 码元级匹配，对全角标点无影响）。
2. **校验中文题面**：检查串需带全角标点（如题面结尾「？」），否则误报 NO。
3. **bash 的 curl 能联网但不能写文件**；下载/验证线上文件用 Python `urllib` 或 `curl.exe`（PowerShell 的 `curl` 是 `Invoke-WebRequest` 别名，不接受 `-sL`）。
4. **Windows 下 `/tmp` 被解析成 `D:\tmp`**；临时文件放当前工作目录或用 git bash `mktemp`。
5. **PowerShell 删含中文路径会静默失败**；删文件用 git bash `rm -rf`。
6. **线上验证 flappy.html 需带浏览器 UA**，默认 UA 会被 CF 403；`index.html` 和 `favicon.svg` 正常 200。
7. **统计题库数量**用 `re.findall(r"q: '([^']+)'", html)`，当前应为 50。
8. **沙盒工作目录限制**：Edit/Write 工具只能在主工作目录内写文件；要写 `D:\个人网站` 需用 Shell + Node.js `fs` 或 PowerShell `[System.IO.File]::WriteAllBytes`。
9. **Tailwind 本地化**：`npx tailwindcss -i input.css -o tailwind.css --minify`，扫描 `index.html` + `flappy.html`，产物约 13KB（CDN 版几百 KB）。
10. **CRLF/LF 混合换行**：原 index.html 部分行是 CRLF，Edit 后可能变 LF；用正则或 `indexOf` 匹配时注意换行符差异。
11. **wrangler 沙箱部署环境变量**：沙箱禁止 wrangler 写 `%APPDATA%\xdg.config\.wrangler\logs` 和项目 `.wrangler\tmp`，需设 `XDG_CONFIG_HOME` 和 `WRANGLER_HOME` 到工作目录内，否则 EPERM 失败。
12. **从线上版本开始改时**：用户说"线上版本"指 https://ycjyybs.pages.dev/ 的真实部署版本，不一定等于本地 Git HEAD。用 `Invoke-WebRequest` 抓线上 HTML 作为基底再改，避免基于过期的本地仓库。
13. **视频背景来源**：`xiong.mp4` 来自 Steam Wallpaper Engine 工坊目录 `E:\SteamLibrary\steamapps\workshop\content\431960\3753326745\`，如需更换背景视频从该目录或其他工坊目录找 .mp4 文件。

---

## 七、未来任务（待接手）

1. **优化文案**：梳理全站文案（Hero 标题/副标题、关于我、兴趣爱好描述、小游戏介绍、留言板引导语等），让表达更自然、有个人风格，避免冗余和重复。
2. **添加好看的动效组件**：在首页引入轻量级动效（如滚动渐入、数字滚动、卡片 hover 微动效、粒子/光效背景等），提升视觉表现力；注意保持性能（不影响背景视频和下雨 Canvas 的帧率）。
3. **增加摄影相册**：新增「摄影」版块，展示摄影作品。可考虑瀑布流/网格布局、灯箱放大、懒加载、按主题分类等；图片需压缩后上传，避免影响加载速度。


---

## 八、接口文档

### 基础信息

| 项 | 值 |
|---|---|
| 基础 URL | `https://ycjyybs.pages.dev/api` |
| 运行时 | Cloudflare Pages Functions（Edge Runtime） |
| 数据库 | Cloudflare D1（SQLite，binding 名 `DB`） |
| 同源校验 | POST/PUT 校验 `Origin` 头，拒绝跨域请求 |
| 速率限制 | 留言 60s/2 条、分数 30s/2 条（按 `CF-Connecting-IP`） |
| 输入限制 | nick ≤20、content ≤100、game ≤20、score ≤100万亿 |

### 留言板接口

#### GET /api/messages
- **功能**：获取最新 100 条留言
- **参数**：无
- **响应**：`200` `[{ id, nick, content, likes, createdAt }, ...]`

#### POST /api/messages
- **功能**：提交留言
- **请求体**：`{ "nick": "昵称", "content": "留言内容" }`
- **校验**：nick 非空 ≤20 字、content 非空 ≤100 字
- **响应**：`201` `{ id, nick, content, likes: 0, createdAt }`
- **错误**：`400` 参数不合法 / `429` 速率超限

#### PUT /api/messages
- **功能**：点赞/取消点赞切换（同 IP 对同一条留言，奇数次=赞，偶数次=取消）
- **请求体**：`{ "id": 留言ID }`
- **响应**：`200` `{ ok: true, liked: boolean, likes: number }`

### 排行榜接口

#### GET /api/scores?game=青蛙&limit=10
- **功能**：获取指定游戏的 TOP 榜单（同名取最高分）
- **参数**：`game`（必填，≤20 字）、`limit`（默认 10，上限 50）
- **响应**：`200` `[{ nick, score, createdAt }, ...]`

#### POST /api/scores
- **功能**：提交分数
- **请求体**：`{ "game": "青蛙", "nick": "昵称", "score": 分数 }`
- **校验**：game/nick 非空、score ≥0 且 ≤100 万亿
- **响应**：`201` `{ ok: true, rank: 排名 }`
- **排名规则**：按该昵称历史最高分计算，超过该分数的不同昵称数 +1

### 统计数据

#### 数据表结构

| 表名 | 用途 | 关键字段 | 索引 |
|---|---|---|---|
| `messages` | 留言板 | id、nick、content、likes、ip、created_at | created_at DESC、ip+created_at |
| `msg_likes` | 点赞去重 | msg_id、ip、created_at | (msg_id, ip) 联合主键、ip+created_at |
| `scores` | 游戏分数 | id、game、nick、score、ip、created_at | game+score DESC |

#### 可统计维度

| 维度 | 查询方式 | 示例 SQL |
|---|---|---|
| 留言总数 | `COUNT(*)` | `SELECT COUNT(*) FROM messages` |
| 今日留言 | 按日期过滤 | `SELECT COUNT(*) FROM messages WHERE date(created_at) = date('now')` |
| 留言总点赞 | `SUM(likes)` | `SELECT SUM(likes) FROM messages` |
| 最高赞留言 | `ORDER BY likes DESC LIMIT 1` | `SELECT * FROM messages ORDER BY likes DESC LIMIT 1` |
| 独立访客数 | `COUNT(DISTINCT ip)` | `SELECT COUNT(DISTINCT ip) FROM messages` |
| 游戏分数总数 | `COUNT(*)` | `SELECT COUNT(*) FROM scores WHERE game = '青蛙'` |
| 最高分 | `MAX(score)` | `SELECT MAX(score) FROM scores WHERE game = '青蛙'` |
| 参与人数 | `COUNT(DISTINCT nick)` | `SELECT COUNT(DISTINCT nick) FROM scores WHERE game = '青蛙'` |
| 活跃 IP 数 | `COUNT(DISTINCT ip)` | `SELECT COUNT(DISTINCT ip) FROM scores` |
| 留言热力 | 按小时分组 | `SELECT strftime('%H', created_at) AS h, COUNT(*) FROM messages GROUP BY h` |

#### IP 隐私说明
- IP 用于速率限制和点赞去重，不展示在前端
- 可通过 `migrate.sql` 定期清理 30 天前的 `msg_likes` 记录
- `messages.ip` 和 `scores.ip` 为永久存储（仅后端可查）
